文件下载和验证码图片输出这两个例子，演示了
使用SERVLET组件来输出动态的，变化的内容，

文件下载是根据用户的选择，例如FILEID=1
来将服务器上的文件输出到客户端。

而验证码是随机生成一个数字，生成一个图片，
然后输出到客户端，另外，还需要将生成的随机
数字写入到“SESSION”，这样，登陆信息，连同
验证码信息发送到服务器后，和“session”中的
验证码进行比较。
验证码这个例子需要大家理解SESSION.

